{
  "registry_version": "agent-reply-schema-v3",
  "contract_version": "3",
  "schema_version": "reply-v3",
  "fields": {
    "reply_text": {
      "kind": "string",
      "min_length": 1,
      "max_length": 2000,
      "required": true,
      "description": "Grounded reply; every assertion about product objects is backed by a ref below."
    },
    "refs": {
      "kind": "list",
      "item": {
        "entity": "string",
        "id": "string"
      },
      "max_items": 10,
      "required": true,
      "description": "Typed product references (listing, criterion, evidence_ref, proposal) populated from tool results; resolved against the session scope at persist (R-14); 0 foreign or invented refs."
    },
    "tool_calls": {
      "kind": "list",
      "item": {
        "tool": "string",
        "args": "object"
      },
      "max_items": 5,
      "required": true,
      "description": "Tool calls of the turn; each tool must be in the compiled intent's allowed_tools policy and args must satisfy its input_schema."
    }
  }
}
