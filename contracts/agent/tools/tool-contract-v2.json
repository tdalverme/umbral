{
  "registry_version": "agent-tool-contract-v2",
  "contract_version": "2",
  "tools": [
    {
      "name": "propose_search_preference_removal",
      "description": "Propone quitar una preferencia suave vigente del radar (p.ej. 'saca la preferencia de luminosidad'); crea una propuesta durable pendiente de confirmacion y no modifica nada hasta confirmar.",
      "mutating": true,
      "requires_confirmation": false,
      "idempotent": true,
      "timeout_seconds": 10,
      "input_schema": {
        "preference": {
          "kind": "string",
          "description": "Frase natural de la preferencia a quitar, p.ej. 'luminosidad', 'con balcon', 'la cocina'. El sistema la traduce deterministicamente al concepto; si no hay una preferencia vigente, falla con preference.not_active."
        }
      },
      "output_schema": {"proposal_id": "uuid", "diff": "object", "impact": "object", "state": "string", "expires_at": "datetime"},
      "output_limits": {"max_items": 1, "forbidden_keys": ["value", "free_feedback"]}
    },
    {
      "name": "get_search_profile",
      "description": "Lee el perfil autorizado de la sesion: snapshot vigente, criterios ejecutables y estado del radar.",
      "mutating": false,
      "requires_confirmation": false,
      "idempotent": false,
      "timeout_seconds": 10,
      "input_schema": {},
      "output_schema": {"profile_id": "uuid", "state": "string", "snapshot": "object", "criteria": "array"},
      "output_limits": {"max_items": 1, "forbidden_keys": ["geometry", "value", "free_feedback"]}
    },
    {
      "name": "list_search_preferences",
      "description": "Lista las preferencias suaves vigentes del radar: concepto, sentido, fuente y fecha; solo lectura.",
      "mutating": false,
      "requires_confirmation": false,
      "idempotent": false,
      "timeout_seconds": 10,
      "input_schema": {},
      "output_schema": {"preferences": "array"},
      "output_limits": {"max_items": 20, "forbidden_keys": ["geometry", "value", "free_feedback"]}
    },
    {
      "name": "propose_search_profile_update",
      "description": "Produce un diff validado con impacto y crea una propuesta durable pendiente; no modifica el perfil.",
      "mutating": true,
      "requires_confirmation": false,
      "idempotent": true,
      "timeout_seconds": 10,
      "input_schema": {
        "change": {
          "kind": "object",
          "description": "Cambio de criterios pedido por el usuario usando claves canonicas; solo campos presentes. Claves validas: zona (string o lista, p.ej. 'Palermo' o ['Nuñez', 'Villa Crespo']), presupuesto o budget (numero, p.ej. 900000), ambientes (numero entero), superficie (numero en m2). No existen radio ni hard_filters en el radar: esos criterios se rechazan."
        }
      },
      "output_schema": {"proposal_id": "uuid", "diff": "object", "impact": "object", "state": "string", "expires_at": "datetime"},
      "output_limits": {"max_items": 1, "forbidden_keys": ["value", "free_feedback"]}
    },
    {
      "name": "propose_search_preference_update",
      "description": "Propone una preferencia suave del radar (luminosidad, balcon, estado general, tipo de cocina) a partir de la frase natural del usuario; crea una propuesta durable pendiente de confirmacion y no modifica nada hasta confirmar.",
      "mutating": true,
      "requires_confirmation": false,
      "idempotent": true,
      "timeout_seconds": 10,
      "input_schema": {
        "preference": {
          "kind": "string",
          "description": "Frase natural de la preferencia tal como la dijo el usuario, p.ej. 'luminoso', 'con balcon', 'cocina separada'. El sistema la traduce deterministicamente al concepto del catalogo; si no hay traduccion, la tool falla con preference.unknown_concept y debes ofrecer el vocabulario soportado."
        }
      },
      "output_schema": {"proposal_id": "uuid", "diff": "object", "impact": "object", "state": "string", "expires_at": "datetime"},
      "output_limits": {"max_items": 1, "forbidden_keys": ["value", "free_feedback"]}
    },
    {
      "name": "apply_search_profile_update",
      "description": "Aplica una propuesta pendiente con confirmacion explicita e idempotency key; versiona el perfil y dispara recomputacion.",
      "mutating": true,
      "requires_confirmation": true,
      "idempotent": true,
      "timeout_seconds": 10,
      "input_schema": {
        "proposal_id": {
          "kind": "uuid",
          "description": "Id de la propuesta pendiente que el usuario confirmo."
        },
        "confirmation": {
          "kind": "boolean",
          "description": "Confirmacion explicita del usuario para aplicar el cambio."
        },
        "idempotency_key": {
          "kind": "string",
          "description": "Clave unica generada por el agente para esta confirmacion; evita duplicados."
        }
      },
      "output_schema": {"proposal_id": "uuid", "state": "string", "profile_version": "integer", "run_id": "uuid"},
      "output_limits": {"max_items": 1, "forbidden_keys": ["value", "free_feedback"]}
    },
    {
      "name": "find_matches",
      "description": "Devuelve los recommendation items persistentes del ultimo run publicado; solo lectura, nunca calcula.",
      "mutating": false,
      "requires_confirmation": false,
      "idempotent": false,
      "timeout_seconds": 10,
      "input_schema": {
        "page": {
          "kind": "integer",
          "description": "Pagina de resultados, empezando en 1."
        },
        "limit": {
          "kind": "integer",
          "description": "Cantidad de items por pagina (1-20)."
        }
      },
      "output_schema": {"run_id": "uuid|null", "items": "array", "total": "integer", "stale": "boolean"},
      "output_limits": {"max_items": 20, "forbidden_keys": ["geometry", "value", "fragment"]}
    },
    {
      "name": "explain_match",
      "description": "Recupera la explicacion persistida de un item y declara datos faltantes e incertidumbre.",
      "mutating": false,
      "requires_confirmation": false,
      "idempotent": false,
      "timeout_seconds": 10,
      "input_schema": {
        "listing_id": {
          "kind": "uuid",
          "description": "Id del listing que el usuario esta viendo o pregunta."
        }
      },
      "output_schema": {"listing_id": "uuid", "score_version": "string", "reasons": "array", "risks": "array", "missing_data": "array", "evidence_refs": "array"},
      "output_limits": {"max_items": 20, "forbidden_keys": ["geometry", "value", "fragment"]}
    },
    {
      "name": "get_listing_detail",
      "description": "Recupera el detalle persistido de un listing del radar de la sesion: precio, expensas, superficie, ambientes, barrio y cambios conocidos.",
      "mutating": false,
      "requires_confirmation": false,
      "idempotent": false,
      "timeout_seconds": 10,
      "input_schema": {
        "listing_id": {
          "kind": "uuid",
          "description": "Id del listing que el usuario esta viendo o pregunta."
        }
      },
      "output_schema": {
        "listing_id": "uuid",
        "source_id": "string",
        "neighborhood": "string|null",
        "geo_precision": "string",
        "total_cost": "number",
        "price_value": "number",
        "price_currency": "string",
        "expenses_value": "number|null",
        "surface_m2": "number|null",
        "rooms": "integer|null",
        "bedrooms": "integer|null",
        "floor": "integer|null",
        "property_type": "string",
        "amenities": "array",
        "known_changes": "array"
      },
      "output_limits": {"max_items": 20, "forbidden_keys": ["geometry", "value", "free_feedback"]}
    },
    {
      "name": "compare_listings",
      "description": "Compara listings del radar de la sesion con la comparacion estructurada persistida; sin ganador generativo.",
      "mutating": false,
      "requires_confirmation": false,
      "idempotent": false,
      "timeout_seconds": 10,
      "input_schema": {
        "listing_ids": {
          "kind": "array",
          "description": "Ids de los listings que el usuario quiere comparar (de su contexto o shortlist)."
        }
      },
      "output_schema": {"comparison": "object", "dimensions": "array", "missing": "array"},
      "output_limits": {"max_items": 20, "forbidden_keys": ["geometry", "value"]}
    },
    {
      "name": "record_feedback",
      "description": "Registra like/dislike con razones opcionales, idempotente; devuelve la propuesta de aprendizaje cuando aplica.",
      "mutating": true,
      "requires_confirmation": false,
      "idempotent": true,
      "timeout_seconds": 10,
      "input_schema": {
        "listing_id": {
          "kind": "uuid",
          "description": "Id del listing que el usuario evalua (el que esta viendo en su contexto)."
        },
        "decision": {
          "kind": "string",
          "enum": ["like", "dislike"],
          "description": "La reaccion del usuario: like si le gusto, dislike si no."
        },
        "reason_keys": {
          "kind": "array",
          "enum": ["price_too_high", "price_fits", "expensas_high", "location_no", "location_yes", "rooms_wrong", "surface_wrong", "building_state", "lighting_bad", "balcony_wanted", "access_ok", "other"],
          "description": "Razones validas del radar; vacio si el usuario no dio razones. Mapea lo que dice el usuario: 'cocina chica' o superficie -> surface_wrong, 'poca luz' -> lighting_bad, 'no me sirve la ubicacion' -> location_no, 'otra razon' -> other."
        },
        "idempotency_key": {
          "kind": "string",
          "description": "Clave unica generada por el agente para este feedback; evita duplicados."
        }
      },
      "output_schema": {"event_id": "uuid", "noop": "boolean", "learning_proposal_id": "uuid|null"},
      "output_limits": {"max_items": 1, "forbidden_keys": ["value", "free_feedback"]}
    },
    {
      "name": "search_urban_context",
      "description": "Consulta signals urbanas versionadas de un listing respetando la precision geografica autorizada.",
      "mutating": false,
      "requires_confirmation": false,
      "idempotent": false,
      "timeout_seconds": 10,
      "input_schema": {
        "listing_id": {
          "kind": "uuid",
          "description": "Id del listing sobre el que se consultan signals urbanas."
        },
        "signal_types": {
          "kind": "array",
          "description": "Tipos de signal a consultar (cafes, transporte, espacios_verdes); vacio = todos."
        }
      },
      "output_schema": {"signals": "array", "precision": "string"},
      "output_limits": {"max_items": 20, "forbidden_keys": ["geometry", "value", "fragment"]}
    }
  ]
}
